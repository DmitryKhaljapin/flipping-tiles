const initialState = {
    selectedTiles: [],
}

export const selectedTilesReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
}
